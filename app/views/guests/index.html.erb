<h1>guests</h1>

<table>

<% for guest in @guests %>
  <tr>
    <td><%= link_to_name guest.person %></td>
    <td><%= link_to_name guest.target %></td>
    <td><%=h guest.status %></td>
    <td><%= link_to 'show', guest %></td>
  </tr>
<% end %>
</table>

<br />

<%= scoped_new_link :guest %>
