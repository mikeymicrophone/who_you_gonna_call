<h1>calls</h1>

<table>

<% for call in @calls %>
  <tr>
    <td><%= link_to call.time.strftime("%I:%M %p %A %m/%d"), call %></td>
    <td><%= link_to_name call.target %></td>
    <td><%=h call.status %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'schedule call', new_call_path %>
